---
- name: Debian install..
  when: ansible_os_family == 'Debian'
  include: Debian.yml
  
- name: EL install...
  when: ansible_os_family == 'RedHat'
  include: EL.yml

- name: upgrading pip
  pip: state=latest name={{item}}
  with_items:
    - pip
    - virtualenv
